import Link from "next/link";
import { CalendarDays, Clock3 } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { StatusBadge } from "@/components/status-badge";
import { getPatientArea } from "@/lib/page-data";
const schedule = "/p/dra-isadora-bezerra/horarios";
export default async function HubPage() { const data = await getPatientArea(); if (!data) return <p role="alert">Não foi possível carregar seu espaço.</p>; const upcoming = data.appointments.filter((item) => new Date(item.starts_at) > new Date() && !["CANCELADO", "EXPIRADO", "REMARCADO"].includes(item.status)).sort((a, b) => a.starts_at.localeCompare(b.starts_at))[0]; return <><PageHeading eyebrow="Espaço do paciente" title={`Olá, ${data.name}`} description="Acompanhe seus agendamentos e dados administrativos." action={<Link href={schedule} className="button-primary">Solicitar horário</Link>} />{upcoming ? <section className="card overflow-hidden"><div className="bg-[#0e6e63] p-5 text-white sm:p-7"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm text-white/65">Próximo agendamento</p><h2 className="mt-2 text-2xl font-black">{upcoming.professionalName}</h2></div><StatusBadge status={upcoming.status} /></div></div><div className="grid gap-5 p-5 sm:grid-cols-2 sm:p-7"><div className="flex gap-3"><CalendarDays className="text-[#117f72]" /><div><p className="muted text-sm">Data</p><p className="font-extrabold">{new Intl.DateTimeFormat("pt-BR", { dateStyle: "long", timeZone: "America/Sao_Paulo" }).format(new Date(upcoming.starts_at))}</p></div></div><div className="flex gap-3"><Clock3 className="text-[#117f72]" /><div><p className="muted text-sm">Horário</p><p className="font-extrabold">{new Intl.DateTimeFormat("pt-BR", { timeStyle: "short", timeZone: "America/Sao_Paulo" }).format(new Date(upcoming.starts_at))}</p></div></div></div><div className="border-t border-[#e4eaed] p-5 sm:px-7"><Link href={`/hub/agendamentos/${upcoming.id}`} className="font-extrabold text-[#117f72]">Ver detalhes →</Link></div></section> : <section className="card p-8 text-center"><h2 className="text-xl font-extrabold">Nenhum agendamento futuro</h2><p className="muted mt-2">Quando Isadora liberar horários, eles aparecerão na área protegida.</p><Link href={schedule} className="button-primary mt-5">Consultar horários</Link></section>}<section className="card mt-6 p-6"><p className="eyebrow">Privacidade</p><h2 className="mt-2 text-lg font-extrabold">Somente informações administrativas</h2><p className="muted mt-2 text-sm leading-6">Este espaço não armazena conteúdo de sessão, diagnóstico ou prontuário clínico.</p></section></>; }
