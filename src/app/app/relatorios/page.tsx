import { Download } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { StatusBadge } from "@/components/status-badge";
import { demoAppointments } from "@/lib/demo-data";

export default function ReportsPage() {
  return <><PageHeading title="Histórico e relatórios" description="Indicadores administrativos sem informações clínicas." action={<button className="button-secondary"><Download size={18} />Exportar CSV</button>} /><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Agendamentos','42'],['Realizados','35'],['Cancelados','4'],['No-show','3']].map(([label,value]) => <article key={label} className="card p-5"><p className="muted text-sm">{label}</p><p className="mt-2 text-3xl font-black">{value}</p></article>)}</section><section className="mt-6 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><div className="card overflow-hidden"><div className="border-b border-[#e6ecef] p-5"><h2 className="font-extrabold">Movimentações recentes</h2></div><div className="divide-y divide-[#e9eef0]">{demoAppointments.map((item) => <div key={item.id} className="grid grid-cols-[1fr_auto] gap-4 p-5"><div><p className="font-extrabold">{item.patient}</p><p className="muted mt-1 text-sm">{item.date.split('-').reverse().join('/')} às {item.start}</p></div><StatusBadge status={item.status} /></div>)}</div></div><div className="card p-5"><h2 className="font-extrabold">Agendamentos por mês</h2><div className="mt-8 flex h-56 items-end justify-around gap-4 border-b border-[#dfe6e9]">{[['Mai',28],['Jun',34],['Jul',38],['Ago',42]].map(([month,value]) => <div key={month} className="flex h-full flex-1 flex-col justify-end text-center"><span className="mb-2 text-sm font-extrabold">{value}</span><div className="mx-auto w-full max-w-14 rounded-t-lg bg-[#42afa9]" style={{height:`${Number(value)*3}px`}} /><span className="mt-3 text-xs text-slate-500">{month}</span></div>)}</div></div></section></>;
}
