import { DashboardRefresh } from "@/components/dashboard-refresh";
import Link from "next/link";
import { CalendarDays, Clock3, UsersRound } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { StatusBadge } from "@/components/status-badge";
import { getProfessionalDashboard } from "@/lib/page-data";

export default async function DashboardPage() {
  const data = await getProfessionalDashboard();
  if (!data) return <p role="alert">Não foi possível carregar os dados.</p>;
  const today = new Intl.DateTimeFormat("pt-BR", { weekday: "long", day: "2-digit", month: "long", timeZone: "America/Sao_Paulo" }).format(new Date());
  const todayKey = new Intl.DateTimeFormat("en-CA", { timeZone: "America/Sao_Paulo" }).format(new Date());
  const todayAppointments = data.appointments.filter((item) => new Intl.DateTimeFormat("en-CA", { timeZone: "America/Sao_Paulo" }).format(new Date(item.starts_at)) === todayKey).length;
  return <><PageHeading eyebrow={today} title={`Olá, ${data.name.split(" ")[0]}`} description="Seu dia, organizado para você se dedicar a cada encontro." action={<Link href="/app/pacientes/novo" className="button-primary">+ Novo paciente</Link>} /><section className="card mb-6 p-5"><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="text-lg font-extrabold">Precisam de você <span className="text-[#117f72]">({data.pending.length})</span></h2><DashboardRefresh /></div><div className="mt-4 grid gap-3">{data.pending.map((item) => <Link className="flex flex-col gap-2 rounded-xl border border-amber-200 bg-amber-50 p-4 sm:flex-row sm:items-center sm:justify-between" href={`/app/agendamentos/${item.id}`} key={item.id}><div><p className="font-extrabold">{item.refund_status === "PENDING" ? "Conferir reembolso" : "Novo atendimento"} · {item.patientName}</p><p className="mt-1 text-sm">{new Intl.DateTimeFormat("pt-BR", { dateStyle: "short", timeStyle: "short", timeZone: "America/Sao_Paulo" }).format(new Date(item.starts_at))}</p></div><span className="text-sm font-bold text-[#117f72]">Ver solicitação →</span></Link>)}{!data.pending.length && <p className="muted text-sm">Tudo em dia por aqui. Quando alguém solicitar um horário, você verá o aviso neste espaço.</p>}</div></section><section className="mb-6 grid gap-4 sm:grid-cols-3"><article className="card p-5"><CalendarDays className="text-[#117f72]" /><p className="muted mt-4 text-sm">Consultas hoje</p><p className="mt-1 text-3xl font-black">{todayAppointments}</p></article><article className="card p-5"><Clock3 className="text-[#117f72]" /><p className="muted mt-4 text-sm">Próximos 14 dias</p><p className="mt-1 text-3xl font-black">{data.appointments.length}</p></article><article className="card p-5"><UsersRound className="text-[#117f72]" /><p className="muted mt-4 text-sm">Pacientes ativos</p><p className="mt-1 text-3xl font-black">{data.patientCount}</p></article></section><div className="mb-6 grid gap-3 sm:grid-cols-2"><Link href="/app/agenda" className="card flex items-center gap-4 p-5 hover:border-[#9ab18c]"><CalendarDays className="text-[#618052]"/><div><p className="font-semibold">Organizar minha agenda →</p><p className="muted mt-1 text-sm">Navegue por dia, semana ou mês e crie um horário.</p></div></Link><Link href="/app/disponibilidade" className="card flex items-center gap-4 p-5 hover:border-[#9ab18c]"><Clock3 className="text-[#618052]"/><div><p className="font-semibold">Definir quando atender →</p><p className="muted mt-1 text-sm">Ajuste dias, horários e bloqueios da sua rotina.</p></div></Link></div><section className="card overflow-hidden"><div className="flex items-center justify-between border-b border-[#e5ebee] p-5"><div><h2 className="text-lg font-extrabold">Próximos agendamentos</h2><p className="muted mt-1 text-sm">Os próximos encontros, em ordem de horário</p></div><Link href="/app/agenda" className="text-sm font-extrabold text-[#117f72]">Ver agenda</Link></div><div className="divide-y divide-[#edf1f3]">{data.appointments.map((item) => <Link href={`/app/agendamentos/${item.id}`} key={item.id} className="grid gap-3 p-5 hover:bg-[#f8fbfb] sm:grid-cols-[150px_1fr_auto] sm:items-center"><p className="font-black text-[#117f72]">{new Intl.DateTimeFormat("pt-BR", { dateStyle: "short", timeStyle: "short", timeZone: "America/Sao_Paulo" }).format(new Date(item.starts_at))}</p><p className="font-extrabold">{item.patientName}</p><StatusBadge status={item.status} /></Link>)}{!data.appointments.length && <p className="muted p-8 text-center">Sua agenda está livre nos próximos 14 dias. Revise os horários disponíveis para receber novas solicitações.</p>}</div></section></>;
}
