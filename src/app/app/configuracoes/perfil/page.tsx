import { PageHeading } from "@/components/page-heading";
import { ProfessionalProfileForm } from "@/components/profile-settings";
import { env } from "@/lib/env";
export default function ProfileSettingsPage() { const payment = env.PAYMENT_PROVIDER_MODE === "fake" ? "Não habilitado" : env.PAYMENT_PROVIDER_MODE === "sandbox" ? "Ambiente de testes" : "Produção"; const whatsapp = env.WHATSAPP_PROVIDER_MODE === "fake" ? "Não habilitado" : env.WHATSAPP_PROVIDER_MODE === "sandbox" ? "Ambiente de testes" : "Produção"; return <><PageHeading title="Perfil profissional" description="Dados administrativos e de exibição da agenda." /><ProfessionalProfileForm /><section className="card mt-6 max-w-4xl p-6"><h2 className="font-extrabold">Integrações</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-[#dfe7ea] p-4"><p className="font-bold">Mercado Pago</p><p className="muted mt-1 text-sm">{payment}</p></div><div className="rounded-xl border border-[#dfe7ea] p-4"><p className="font-bold">WhatsApp</p><p className="muted mt-1 text-sm">{whatsapp}</p></div></div></section></>; }
