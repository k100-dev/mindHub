"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { CalendarDays, Home, UserRound, Plus } from "lucide-react";
import { Brand } from "@/components/brand";
import { SignOutButton } from "@/components/sign-out-button";
const links=[{href:"/hub",label:"Meu espaço",icon:Home},{href:"/hub/agendamentos",label:"Meus encontros",icon:CalendarDays},{href:"/hub/perfil",label:"Meu perfil",icon:UserRound}];
export function HubShell({children}:{children:React.ReactNode}){const path=usePathname();return <div className="patient-space min-h-screen"><header className="sticky top-0 z-20 border-b border-[#dfe7df] bg-[#fcfdfb]/95 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between gap-3 px-5 py-4 sm:px-8"><Brand/><nav className="hidden items-center gap-2 md:flex" aria-label="Navegação do paciente">{links.map(({href,label})=><Link key={href} href={href} aria-current={(href==='/hub'?path===href:path.startsWith(href))?'page':undefined} className="patient-nav-link">{label}</Link>)}</nav><div className="flex items-center gap-3"><Link href="/hub/agendar" aria-label="Agendar encontro" className="button-primary !rounded-full !px-4 text-sm"><Plus size={17}/><span className="hidden sm:inline">Agendar</span></Link><SignOutButton compact/></div></div></header><main className="mx-auto max-w-6xl px-5 py-9 pb-28 sm:px-8 sm:py-12 md:pb-12"><div key={path} className="page-enter">{children}</div></main><nav aria-label="Navegação móvel" className="patient-bottom-nav md:hidden">{links.map(({href,label,icon:Icon})=><Link key={href} href={href} aria-current={(href==='/hub'?path===href:path.startsWith(href))?'page':undefined}><Icon size={21}/><span>{label}</span></Link>)}</nav></div>}
